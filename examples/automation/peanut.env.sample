# Copy this file to a machine-local secret path such as /opt/peanut/peanut.env
# Then replace the placeholder values before running cron/systemd/CI jobs.

BASE_URL=http://127.0.0.1:3492
APP_ID=default
SERVICE_TOKEN=pst_replace_me
TABLE_NAME=ops_todos
STORAGE_BUCKET=assets
STORAGE_KEY=ops/hello.txt
AUTOMATION_OUT_DIR=/opt/peanut/backups
